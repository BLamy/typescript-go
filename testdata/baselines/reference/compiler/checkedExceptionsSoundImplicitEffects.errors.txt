checkedExceptionsSoundImplicitEffects.ts(16,12): error TS100021: Call may throw 'unknown', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(25,5): error TS100021: Call may throw 'unknown', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(32,5): error TS100021: Call may throw 'unknown', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(38,12): error TS100021: Call may throw 'unknown', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(49,12): error TS100021: Call may throw '"construct"', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(58,12): error TS100022: Thrown value of type '"read accessor"' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(61,5): error TS100022: Thrown value of type '"write accessor"' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(69,5): error TS100021: Call may throw 'unknown', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(76,5): error TS100021: Call may throw 'unknown', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(83,11): error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(92,11): error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(109,12): error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(112,12): error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(122,13): error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(125,5): error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(132,12): error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(135,32): error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(141,5): error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
checkedExceptionsSoundImplicitEffects.ts(147,6): error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.


==== checkedExceptionsSoundImplicitEffects.ts (19 errors) ====
    // Minimal global required by the explicit-resource-management grammar in this
    // focused baseline; the value below is null, so no member shape is needed.
    interface Disposable {}
    
    declare function safe(): void throws never;
    declare function known(): void throws "known";
    
    // A precisely declared direct call remains usable in strict code.
    function directSafeCall(): void throws never {
        safe();
    }
    
    // Existing standard-library declarations have no throws metadata, so strict
    // mode treats them as unknown rather than claiming they are safe.
    function parseJson(): unknown throws never {
        return JSON.parse("{");
               ~~~~~~~~~~~~~~~
!!! error TS100021: Call may throw 'unknown', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    
    // Fetching a method may itself invoke a getter or Proxy trap, even when the
    // resolved method signature explicitly promises `throws never`.
    declare const service: {
        run(): void throws never;
    };
    function methodLookup(): void throws never {
        service.run();
        ~~~~~~~~~~~~~
!!! error TS100021: Call may throw 'unknown', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    
    declare const keyedService: {
        run(): void throws never;
    };
    function elementLookup(): void throws never {
        keyedService["run"]();
        ~~~~~~~~~~~~~~~~~~~~~
!!! error TS100021: Call may throw 'unknown', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    
    // Construct signatures without throws metadata are unknown boundaries.
    declare const LegacyConstructor: new () => object;
    function constructLegacy(): object throws never {
        return new LegacyConstructor();
               ~~~~~~~~~~~~~~~~~~~~~~~
!!! error TS100021: Call may throw 'unknown', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    
    // Construct signatures can opt into precise contracts without rewriting the
    // rest of a legacy declaration surface.
    declare const SafeConstructor: new () => object throws never;
    declare const KnownConstructor: new () => object throws "construct";
    function constructSafe(): object throws never {
        return new SafeConstructor();
    }
    function constructKnownInNever(): object throws never {
        return new KnownConstructor();
               ~~~~~~~~~~~~~~~~~~~~~~
!!! error TS100021: Call may throw '"construct"', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    
    // Accessor declarations carry distinct read and write effects.
    declare const declaredAccessor: {
        get value(): number throws "read accessor";
        set value(next: number) throws "write accessor";
    };
    function readDeclaredAccessor(): number throws never {
        return declaredAccessor.value;
               ~~~~~~~~~~~~~~~~~~~~~~
!!! error TS100022: Thrown value of type '"read accessor"' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    function writeDeclaredAccessor(): void throws never {
        declaredAccessor.value = 1;
        ~~~~~~~~~~~~~~~~~~~~~~
!!! error TS100022: Thrown value of type '"write accessor"' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    
    // An untracked constituent poisons a union call instead of disappearing.
    type KnownFunction = () => void throws "known";
    type LegacyFunction = () => void;
    declare const unionFunction: KnownFunction | LegacyFunction;
    function callUnion(): void throws "known" {
        unionFunction();
        ~~~~~~~~~~~~~~~
!!! error TS100021: Call may throw 'unknown', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    
    // The selected legacy overload is unknown.
    declare function overloaded(value: 0): void throws "known";
    declare function overloaded(value: number): void;
    function callLegacyOverload(): void throws never {
        overloaded(1);
        ~~~~~~~~~~~~~
!!! error TS100021: Call may throw 'unknown', which is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    
    // Class evaluation can execute static initializers, computed names, and
    // decorators. It remains unknown until those effects are modeled separately.
    declare function legacy(): void;
    function defineClass(): void throws never {
        class Local {
              ~~~~~
!!! error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
            static value = legacy();
        }
    }
    
    // A dynamic base may fail IsConstructor while the class definition is being
    // evaluated; merely reading the identifier is not the whole class effect.
    declare const dynamicBase: any;
    function defineDynamicSubclass(): void throws never {
        class Local extends dynamicBase {}
              ~~~~~
!!! error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    
    // Catching an unknown standard-library boundary is sound, but the binding must
    // remain unknown.
    function catchJson(): void throws never {
        try {
            JSON.parse("{");
        } catch (e) {
            const value: unknown = e;
        }
    }
    
    // Operators that can perform user coercion fail closed, while strict equality
    // and logical negation remain proven-safe operations.
    declare const dynamicValue: any;
    function coercion(): unknown throws never {
        return dynamicValue + 1;
               ~~~~~~~~~~~~~~~~
!!! error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    function unaryCoercion(): number throws never {
        return +dynamicValue;
               ~~~~~~~~~~~~~
!!! error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    function safePredicates(value: unknown): boolean throws never {
        return !(value === null);
    }
    
    // Iteration, spreading, tagged templates, and parameter destructuring all
    // execute protocol hooks or property reads that can throw.
    declare const iterable: Iterable<number>;
    function spreadIterable(): number[] throws never {
        return [...iterable];
                ~~~~~~~~~~~
!!! error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    function iterate(): void throws never {
        for (const value of iterable) {
        ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
            safe();
    ~~~~~~~~~~~~~~~
        }
    ~~~~~
!!! error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    
    declare const tag: (parts: TemplateStringsArray) => string throws never;
    function taggedTemplate(): string throws never {
        return tag`value`;
               ~~~~~~~~~~
!!! error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    
    function destructuredParameter({ value }: { value: number }): void throws never {
                                   ~~~~~~~~~
!!! error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
        safe();
    }
    
    // Explicit resource management invokes a disposal hook at scope exit.
    function disposalAtScopeExit(): void throws never {
        using resource = null;
        ~~~~~~~~~~~~~~~~~~~~~
!!! error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    
    declare const destructuringSource: { value: number };
    let assignedValue: number;
    function destructuringAssignment(): void throws never {
        ({ value: assignedValue } = destructuringSource);
         ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! error TS100022: Thrown value of type 'unknown' is neither caught by an enclosing 'try' statement nor declared in the enclosing function's 'throws' clause.
    }
    