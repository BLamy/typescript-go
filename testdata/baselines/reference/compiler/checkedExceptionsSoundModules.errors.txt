main.ts(3,1): error TS100025: Thrown value of type 'unknown' is not caught by an enclosing 'try' statement. Code outside of a function must handle all checked exceptions.


==== dependency.ts (0 errors) ====
    export const value = 1;
    
==== main.ts (1 errors) ====
    // Static module evaluation is an effect boundary. Until modules can publish an
    // initialization effect, checked exceptions must not silently call an import safe.
    import { value } from "./dependency";
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
!!! error TS100025: Thrown value of type 'unknown' is not caught by an enclosing 'try' statement. Code outside of a function must handle all checked exceptions.
    value;
    